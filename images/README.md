# images

記事用の画像を保存するディレクトリです。

- 各記事ごとに `<slug>/` のサブディレクトリを作成し、その記事で使用する画像ファイルを収納してください。
  - 例: `howto-ansys-modal-analysis/images/figure-01.png`
- 画像ファイル名は `figure-01.png`, `figure-02.svg` のように連番で付けると管理しやすくなります。
- 記事本文からは相対パスで画像を参照します。
